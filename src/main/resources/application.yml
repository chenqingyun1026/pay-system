spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/mall?userUnicode=true&characterEncoding=utf8&serverTimezone=UTC
mybatis:
  mapper-locations: classpath:mapper/*.xml